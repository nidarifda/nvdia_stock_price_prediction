# ===== Backend runtime settings =====
# Directory that contains your saved models
MODEL_DIR=models

# Choose feature view: A (NVDA-only) | B (All) | AFF (Affiliates)
DEFAULT_TAG=B

# Choose model family: lgbm | lstm | bilstm
DEFAULT_FRAMEWORK=lgbm

# Classification threshold for "UP" label
THRESHOLD_UP=0.50

# Optional: path to scaler if you inverse-transform regression outputs
Y_SCALER_PATH=models/y_scaler.pkl

# ===== CORS =====
# Comma-separated list of allowed origins (no trailing slashes)
# Add your deployed frontend origin(s) here.
CORS_ALLOW_ORIGINS=http://localhost:5173,https://<your-username>.github.io,https://<your-username>.github.io/<repo-name>,https://<your-namespace>.gitlab.io

# ===== Uvicorn =====
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# ===== Notes =====
# 1) Copy this file to `.env` and edit values for local/dev use.
# 2) On Render, set these as Environment Variables in the dashboard.
# 3) For the React app, set VITE_API_BASE in the frontend build (e.g., GitHub Actions secret or GitLab CI var).
